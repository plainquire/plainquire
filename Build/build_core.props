<Project>
  <Import Project="analyzer_disposeable.props" />
  <Import Project="analyzer_meziantou.props" />

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <DefineConstants Condition="'$(TargetFramework)' == 'net8.0'">$(DefineConstants);NET_CORE</DefineConstants>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  
</Project>